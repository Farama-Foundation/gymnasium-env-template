[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "{{ environment_name }}"
version = "0.0.1"
dependencies = [
  "gymnasium",
  "pygame==2.1.3",
  "pre-commit",
]
